@{
    ViewData["Title"] = "Профиль";
}

<div class="d-flex justify-content-center align-items-center vh-100 rounded" style="background-color: white;">
            <div class="card shadow-lg p-4" style="width: 1000px; border-radius: 15px; background-color: white;">
        @inject IHttpContextAccessor HttpContextAccessor
        <h2>Привет, @HttpContextAccessor.HttpContext.Session.GetString("UserName")!</h2>

        <form asp-controller="Auth" asp-action="Logout" method="post">
            <button type="submit" class="btn btn-outline-light mt-3">Выйти</button>
        </form>
        </div>
    </div>
